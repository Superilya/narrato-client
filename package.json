{
  "name": "narrato-client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "rm -rf build && ./node_modules/typescript/bin/tsc -w",
    "server": "nodemon ./build/index.js",
    "build": "rm -rf buiold && ./node_modules/typescript/bin/tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/isomorphic-fetch": "0.0.35",
    "@types/node-telegram-bot-api": "^0.50.4",
    "nodemon": "^2.0.6",
    "typescript": "^4.0.5"
  },
  "dependencies": {
    "isomorphic-fetch": "^3.0.0",
    "node-telegram-bot-api": "^0.50.0"
  }
}
